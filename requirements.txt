# Core dependencies for EVE Fleet MCP Server
requests==2.32.3
aiohttp==3.12.15
pyyaml==6.0.1
python-jose==3.3.0

# MCP framework (also specified in pyproject.toml)
mcp[cli]>=1.14.0

# Sub-dependencies (automatically installed but pinned for reproducibility)
# JWT validation dependencies
ecdsa==0.19.0
pyasn1==0.4.8
rsa==4.9
six==1.16.0

# HTTP client dependencies
certifi>=2024.12.14
charset-normalizer>=3.0.0
idna>=3.7
urllib3>=2.2.0

# Async HTTP dependencies
anyio>=4.0.0
sniffio>=1.0.0
platformdirs>=4.2.2
